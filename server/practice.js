const first = "female  ";
const second = "female     ";

// if (first.toString().trim() === second.toString().trim()) {
//   console.log('data are the same');
// } else {
//   console.log('data are not the same');
// }

const datas = [12, 34, 5, 7, 9, 55, 77, 8, 8, 9, 0];

const returner = (dt) => {
  return dt;
};

const data2 = returner(datas);

console.log(data2);
