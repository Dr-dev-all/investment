import { CSSProperties } from 'react';
import { TransitionStrategyProps } from '../types';
export declare function flipTransition({ orientation, currentPage, slidesPerPage, slideIndex, slidePercentage, slidesGap, isActive, }: TransitionStrategyProps): CSSProperties;
export declare const flipTransitionWrapperStyles: CSSProperties;
