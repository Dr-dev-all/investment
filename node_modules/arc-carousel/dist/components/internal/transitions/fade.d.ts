import { CSSProperties } from 'react';
import { TransitionStrategyProps } from '../types';
export declare function fadeTransition({ slideIndex, slidePercentage, slidesPerPage, slidesGap, isActive, orientation, }: TransitionStrategyProps): CSSProperties;
