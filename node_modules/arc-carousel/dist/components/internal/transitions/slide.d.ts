import { CSSProperties } from 'react';
import { TransitionStrategyProps } from '../types';
export declare function slideTransition({ orientation, gap, currentPage, slidesPerPage, }: TransitionStrategyProps): CSSProperties;
