import { Breakpoints } from '../../../types/Breakpoints';
type BreakpointsMap<T> = Partial<Record<Breakpoints, T>> & {
    base: T;
};
export declare function useBreakpointValue<T>(breakpoints: BreakpointsMap<T>): T;
export {};
