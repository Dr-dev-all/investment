import { CSSProperties } from 'react';
import { Orientation, Transition } from '../types';
type UseSlideStyleProps = {
    orientation: Orientation;
    transition: Transition;
    gap: number;
    currentPage: number;
    slidesPerPage: number;
    slideIndex: number;
    isActive: boolean;
};
export declare function useSlideStyle({ orientation, transition, gap, currentPage, slidesPerPage, slideIndex, isActive, }: UseSlideStyleProps): CSSProperties;
export {};
